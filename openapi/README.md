# Custom GPT write action

Import `gpt-write-action.yaml` in the Custom GPT editor and configure GitHub App OAuth as described in `docs/custom-gpt-write-setup.md`. The surface is intentionally limited to creating and reading Issues in one fixed repository; it exposes no content-writing operation.
